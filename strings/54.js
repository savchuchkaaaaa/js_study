let students = prompt('Enter the number of students: ')
let chairs = prompt('Enter the number of chairs: ')
if (chairs >= students) {
    alert('The number of chairs is enough')
} else alert('The number of chairs is not enough')