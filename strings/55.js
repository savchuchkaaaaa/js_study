let students = prompt('Enter the number of students: ')
let desks = prompt('Enter the number of desks: ')
if (desks * 2 >= students) {
    alert('The number of desks is enough')
} else alert('The number of desks is not enough')